# Sécurité

## Exécution des outils (conteneur Kali)

L'exécution des commandes ne passe jamais par le shell de l'hôte ni par `docker.sock` :

```
cyberlab-api / cyberlab-worker --(HTTP interne, réseau cyberlab-kali-net)--> cyberlab-kali (agent FastAPI, port 9000)
```

- **Allowlist stricte des exécutables** : `kali/agent/main.py` résout au démarrage le chemin absolu de chaque outil autorisé — 31 outils curés depuis la Phase 12 (voir [tools.md](tools.md) pour la liste complète et le détail des exclusions), 3 au MVP — via `shutil.which`. Une requête ne peut jamais désigner un autre binaire — seul un nom logique (`"tool": "nmap"`) est accepté, jamais un chemin. Ce dictionnaire (`CANDIDATE_TOOLS`) doit rester en synchronisation avec les définitions YAML côté API (`backend/app/tools/definitions/`) — les deux couches doivent accepter un outil pour qu'il soit réellement exécutable.
- **Pas de shell** : `subprocess.run([...], shell=False)` uniquement, arguments passés en liste (jamais de concaténation de chaîne).
- **Validation des arguments** : rejet des métacaractères shell (`; & | \` $ < > \n \r \\`) même si `shell=False` ne les interprète pas — défense en profondeur. Longueur et nombre d'arguments plafonnés. Voir `kali/agent/tests/test_validation.py`.
- **Authentification interne** : un secret partagé (`KALI_AGENT_TOKEN`, généré aléatoirement dans `.env`) est requis en en-tête `X-Agent-Token`. Le conteneur Kali n'est de toute façon accessible que depuis le réseau isolé `cyberlab-kali-net` (api + worker uniquement, aucun port publié sur l'hôte).
- **Timeout obligatoire** : plafonné à 300s côté agent, quel que soit ce que demande l'appelant.
- **Isolation du conteneur** : `cap_drop: ALL`, `security_opt: no-new-privileges`, utilisateur non-root, pas d'accès à `docker.sock`, pas de `privileged: true`, limites mémoire/CPU.

### Conséquence connue : pas de scan SYN par défaut, `-Pn` obligatoire

`cap_drop: ALL` retire `CAP_NET_RAW`, nécessaire aux scans nmap bas niveau (`-sS`, découverte ARP/ICMP, etc.). Par défaut, les jobs nmap utilisent `-sT` (TCP connect, pas de socket brut — validé en Phase 2) **et `-Pn`** (désactive la découverte d'hôte, qui utilise ICMP/ARP par défaut et nécessite donc aussi `CAP_NET_RAW`, indépendamment de `-sT`).

Trouvé en Phase 9 en scannant un vrai lab (DVWA) plutôt que le conteneur Kali lui-même : sans `-Pn`, nmap échoue avec `Couldn't open a raw socket or eth handle` dès la phase de découverte, avant même le scan de ports — ce n'était pas visible plus tôt car les tests précédents ciblaient `cyberlab-kali` depuis lui-même, un cas limite proche du loopback où nmap ne déclenche pas la découverte réseau classique. `-Pn` traite systématiquement la cible comme active et va directement au scan de ports, ce qui est le comportement voulu ici (les cibles sont des conteneurs sur des réseaux Docker contrôlés, pas un scan Internet où "the host might not respond to ping" justifierait une vraie découverte).

**Mise à jour Phase 12** : `cap_add: [NET_RAW]` a été ajouté au service `cyberlab-kali` — pas en remplacement de `cap_drop: ALL`, en ajout narrow par-dessus. Nécessaire parce que deux outils du nouvel arsenal n'ont *aucun* mode non privilégié : `masscan` (envoi de paquets SYN bruts, contrairement à nmap qui a `-sT`) et `traceroute` (sondes ICMP/UDP brutes). `arp-scan`/`netdiscover` (couche ARP) ont aussi été ajoutés au registre malgré un besoin de privilèges probablement plus large (`CAP_NET_RAW` peut ne pas suffire selon la façon dont le paquet Kali applique ses capabilities de fichier) — leur statut réel est visible via `GET /api/tools/health`, qui reste honnête si l'invocation échoue plutôt que de le masquer. `nmap` continue d'utiliser `-sT`/`-Pn` par défaut (comportement inchangé) ; `CAP_NET_RAW` étant maintenant présent, un `-sS` deviendrait possible mais n'a pas été activé par défaut — resterait un choix à faire explicitement dans une définition de profil future.

## Terminal intégré — surface la plus privilégiée de l'application

```
Nuxt (xterm.js) --WS--> cyberlab-api (relais transparent) --WS interne--> cyberlab-kali (PTY)
```

Contrairement au Job Engine (arguments strictement validés, allowlist d'outils), le terminal donne un **shell interactif complet** — `bash` dans le conteneur `cyberlab-kali`, sans allowlist de commandes. C'est intentionnel (spec section 7) mais cela en fait la surface la plus sensible de CyberLab :

- **Toujours confiné au conteneur Kali** : `cyberlab-api` (`backend/app/api/routes/terminal.py`) ne fait que relayer des frames WebSocket entre le navigateur et l'agent Kali — il n'ouvre jamais lui-même de PTY ni de shell. Le PTY est ouvert par `kali/agent/main.py` (`pty.openpty()` + `subprocess.Popen(["bash"], preexec_fn=os.setsid, ...)`), à l'intérieur du conteneur durci (voir ci-dessus : `cap_drop: ALL`, non-root, pas de `docker.sock`).
- **Authentification interne** : la connexion WebSocket `cyberlab-api → cyberlab-kali` porte le même `KALI_AGENT_TOKEN` que l'exécution d'outils. Testé (`kali/agent/tests/test_terminal_auth.py`) : token manquant, incorrect, ou agent démarré sans `AGENT_TOKEN` configuré → `close(code=4401)` dans les trois cas (pas de repli silencieux vers "auth désactivée").
- **Authentification** : par défaut (`AUTH_ENABLED=false`), n'importe qui atteignant `WS /api/ws/terminal` obtient un shell dans le conteneur Kali — acceptable uniquement parce que CyberLab n'écoute que sur `127.0.0.1` par défaut. Avec `AUTH_ENABLED=true` (voir « Authentification API » ci-dessous), cet endpoint exige le même bearer token que le reste de l'API, requis en prérequis **avant toute exposition au-delà de localhost**.
- **Cycle de vie du processus** : le PTY et le processus `bash` sont fermés/tués (`proc.terminate()` + `proc.wait()`) à la déconnexion — un bug de zombie process (le shell restait `<defunct>` faute de `wait()`) a été trouvé et corrigé pendant les tests de la Phase 6.

## Lab Manager — accès Docker via un Docker Socket Proxy, plus de `docker.sock` direct

Le Lab Manager (démarrer/arrêter/reset/supprimer des labs Docker vulnérables-par-design comme DVWA) a besoin de contrôler Docker. C'est en tension directe avec la règle « ne jamais donner `docker.sock` au backend ». Résolution retenue :

- **`docker.sock` n'est monté (en lecture seule) que dans `cyberlab-docker-proxy`** (`tecnativa/docker-socket-proxy`), un service dédié qui n'expose qu'un sous-ensemble restreint de l'API Docker en HTTP sur le réseau interne `cyberlab-backend` : `CONTAINERS`, `NETWORKS`, `IMAGES`, `POST` activés (ce dont `docker_manager.py` a besoin pour créer/démarrer/arrêter/supprimer les conteneurs et réseaux de labs) ; `EXEC`, `VOLUMES`, `SYSTEM`, `BUILD`, `SECRETS`, `SERVICES`, `SWARM`, `AUTH`, etc. explicitement désactivés (`docker-compose.yml`).
- **`cyberlab-labmanager` n'a plus aucun accès au socket** : il pointe simplement `DOCKER_HOST=tcp://cyberlab-docker-proxy:2375` — le SDK Docker Python (`docker.from_env()`, `labmanager/docker_manager.py:19`) respecte cette variable exactement comme le ferait le CLI Docker, donc aucun changement de code n'a été nécessaire pour cette bascule. `cyberlab-api` et `cyberlab-worker` n'ont jamais accès à Docker ni au proxy — ils passent par `POST /api/labs/...`, qui relaie (`backend/app/api/routes/labs.py`) vers le Lab Manager via HTTP interne authentifié (`LABMANAGER_TOKEN`, même modèle que l'agent Kali).
- **Pourquoi un proxy plutôt que suppression pure et simple** : le Lab Manager doit rester capable de créer/démarrer/arrêter des conteneurs et réseaux Docker à la demande — il n'existe pas d'alternative sans passer par l'API Docker sous une forme ou une autre. Le proxy ne supprime donc pas le besoin, mais réduit drastiquement ce qu'une compromission de `cyberlab-labmanager` permettrait de faire : même avec un accès complet au proxy, un attaquant ne peut pas `docker exec` dans un conteneur existant (donc pas de pivot direct vers `cyberlab-api`, `cyberlab-postgres`, etc. via leurs conteneurs), ne peut pas monter de volume arbitraire (donc pas de lecture/écriture hors des conteneurs qu'il crée lui-même), et n'a aucun accès aux endpoints `SYSTEM`/`SECRETS`/`SWARM`. Il reste capable de créer des conteneurs sur le réseau `cyberlab-backend` — un vecteur résiduel documenté ci-dessous, pas éliminé.
- **Conséquence positive supplémentaire** : n'ayant plus besoin d'un accès en lecture/écriture au fichier socket de l'hôte (dont la propriété/groupe imposait `root` dans le conteneur), `cyberlab-labmanager` tourne maintenant en utilisateur non-root (`labmanager/Dockerfile`), avec `cap_drop: ALL` et `security_opt: no-new-privileges` — aligné sur le reste de la stack.
- **Risque résiduel accepté** : `cyberlab-docker-proxy` lui-même a toujours un accès complet en lecture au socket réel et peut créer des conteneurs/réseaux sans restriction de configuration (pas d'allowlist d'image, pas de limite de ressources imposée au niveau du proxy) — la validation de ce qui peut être lancé (uniquement les définitions de labs allowlistées dans `labmanager/definitions/`) reste donc une responsabilité applicative de `docker_manager.py`, pas une garantie du proxy. Compromettre `cyberlab-labmanager` *et* franchir cette validation applicative resterait équivalent, en pratique, à un accès Docker large sur l'hôte — d'où le choix de garder ce service minuscule, sans exécution de code arbitraire, plutôt que de considérer le proxy comme une garantie suffisante à lui seul.
- **Chaque lab est isolé** : réseau Docker dédié (`cyberlab-lab-<id>`), en plus d'être connecté à `cyberlab-kali-net` pour que le conteneur Kali puisse le scanner par nom. Port hôte publié uniquement sur `127.0.0.1`, port aléatoire (jamais de port fixe qui pourrait entrer en collision ou être deviné).
- **Découverte fiable via labels Docker** : le Lab Manager ne maintient pas d'état dupliqué en base — il interroge Docker (via le proxy) directement (`docker ps --filter label=cyberlab.lab=true`), donc pas de désynchronisation possible entre l'état réel des conteneurs et ce que l'UI affiche.
- **Bug trouvé et corrigé pendant les tests de la Phase 7** : les appels du SDK Docker sont synchrones ; les exécuter directement dans un handler FastAPI `async def` bloque tout l'event loop pendant un pull d'image (potentiellement plusieurs minutes) — y compris `/health`, rendant le service entièrement indisponible entre-temps. Corrigé en déportant chaque appel Docker SDK dans un thread (`loop.run_in_executor`).
- **Vérifié en conditions réelles (Phase 11)** : cycle de vie complet d'un lab DVWA (création → conteneur `running` avec port publié sur `127.0.0.1` → réponse HTTP 302 réelle du service → arrêt → suppression du conteneur et du réseau dédié) rejoué intégralement via le proxy après la bascule, sans aucun changement à `docker_manager.py`.

## IA — l'IA ne peut jamais exécuter d'action directement

Le Mission Planner (`backend/app/ai/planner.py`) **propose** un plan ; il n'appelle jamais `POST /api/jobs` lui-même. C'est le frontend, sur action explicite de l'utilisateur (bouton « Run » par étape), qui déclenche l'exécution — via le même endpoint et donc la **même validation stricte du Tool Registry** que la page Tools (voir [ai.md](ai.md) pour des exemples réels d'options hallucinées par le modèle et rejetées par le registre avant tout appel à l'agent Kali). Un nom d'outil halluciné (qui n'existe pas dans le registre) est explicitement supprimé de l'étape (`step.tool = None`) plutôt que transmis tel quel.

**Phase 12 — `ai_allowed` à deux niveaux** : les outils marqués `ai_allowed: false` dans leur définition (`sqlmap`, seul cas actuellement — voir [tools.md](tools.md)) sont filtrés **avant même la construction du prompt** envoyé au modèle : `ai_visible_tools = [t for t in all_tools if t.ai_allowed]`, et seule cette liste filtrée alimente `build_planner_prompt()`. Le modèle n'est donc jamais informé que `sqlmap` existe dans ce déploiement. Défense en profondeur : même si le modèle propose quand même `"sqlmap"` — appris de ses données d'entraînement générales plutôt que du prompt fourni — la réponse est revalidée après coup contre le même filtre `ai_allowed`, exactement comme un nom halluciné. Un nom de profil (`step.profile`) invalide ou inexistant pour l'outil subit le même traitement (`step.profile = None`, retombe sur des `options` brutes). Testé de façon adversariale avec un faux provider qui propose spécifiquement `sqlmap` (un vrai nom d'outil enregistré, pas une hallucination comme `bash`/`metasploit`) pour prouver que le filtre `ai_allowed` est une vérification indépendante du filtre "outil existe" — voir `backend/tests/ai/test_ai_security_boundary.py::test_ai_planner_strips_registered_but_not_ai_allowed_tool`.

## Authentification API (`AUTH_ENABLED`)

Désactivée par défaut (`AUTH_ENABLED=false` dans `.env.example`) : CyberLab n'écoute que sur `127.0.0.1` par défaut, donc pas de chemin réseau non fiable vers l'API. `API_SECRET_KEY` (déjà présent depuis la Phase 1, jamais branché avant la Phase 10) sert de bearer token dès que `AUTH_ENABLED=true`.

- **Middleware ASGI pur** (`backend/app/core/auth_middleware.py`), pas un `BaseHTTPMiddleware` — nécessaire pour pouvoir aussi protéger les upgrades WebSocket, pas seulement les requêtes HTTP classiques.
- **`/api/health` reste toujours accessible sans token** (healthcheck Docker non authentifié par nature — voir `docker-compose.yml`).
- **HTTP** : `Authorization: Bearer <API_SECRET_KEY>`.
- **WebSocket** : un navigateur ne peut pas fixer d'en-tête personnalisé sur un handshake WS — le token est donc accepté en paramètre de requête (`?token=...`) pour `/api/ws/jobs/{id}` et `/api/ws/terminal`. Rejet conforme à la spec ASGI : l'événement `websocket.connect` initial est bien reçu avant l'envoi de `websocket.close(code=4401)` (sinon certains serveurs ASGI se plaignent d'une réponse avant réception du premier message).
- **Frontend** : `docker-compose.yml` calcule `NUXT_PUBLIC_API_TOKEN` à partir de `API_SECRET_KEY` uniquement si `AUTH_ENABLED` est défini (`${AUTH_ENABLED:+${API_SECRET_KEY}}`) ; `useApi()` (`frontend/app/composables/useApi.ts`) ajoute automatiquement l'en-tête `Authorization` sur toutes les requêtes (`apiFetch`) et le paramètre `?token=` sur les WebSocket et les téléchargements de rapports (`<a href>`, qui ne peuvent pas non plus porter d'en-tête personnalisé).
- **Testé** (`backend/tests/test_auth_middleware.py`, 8 tests) : désactivé par défaut, `/api/health` toujours joignable, route protégée rejetée sans token / avec mauvais token / acceptée avec le bon token, chemins hors `/api` non gardés, WebSocket rejetée sans token (code `4401`) et acceptée avec `?token=`.
- **Vérifié en conditions réelles** : stack complète relancée avec `AUTH_ENABLED=true`, confirmé que `curl` sans token reçoit `401`, avec le bon token `200`, et que l'interface Nuxt continue de fonctionner de façon transparente (dashboard, lancement d'un scan, mise à jour temps réel du statut via WebSocket) sans aucun changement visible pour l'utilisateur.

## Réseau

- Tous les ports hôte sont bindés sur `127.0.0.1` uniquement.
- Réseau `cyberlab-kali-net` séparé de `cyberlab-backend` : seuls `cyberlab-api`, `cyberlab-worker`, et les conteneurs de labs (connectés dynamiquement par le Lab Manager) peuvent atteindre `cyberlab-kali`.
- `cyberlab-labmanager` n'est que sur `cyberlab-backend` — pas de raison qu'il touche `cyberlab-kali-net`.

## Secrets

- Aucun secret en dur dans le code ou committé dans Git (`.env` est dans `.gitignore`).
- `API_SECRET_KEY`, `POSTGRES_PASSWORD`, `KALI_AGENT_TOKEN`, `LABMANAGER_TOKEN` générés aléatoirement (`openssl rand -hex`) dans l'environnement de développement local.

## Phase 10 — Audit de durcissement

### Fait pendant cet audit

- **Authentification API optionnelle** ajoutée (voir ci-dessus) — fermait le dernier écart de la spec section 21 (« authentification si exposée au réseau »).
- **XSS trouvée et corrigée dans les rapports HTML** : `html_renderer.py` utilisait `jinja2.Template(...)` sans `autoescape=True`. Les titres/descriptions de findings peuvent refléter du contenu venant de la cible scannée (sortie d'outil, ex. nikto renvoyant un `<script>` brut trouvé sur une page) — sans échappement, ce contenu s'injectait tel quel dans le rapport HTML. Corrigé avec `autoescape=True` ; test de non-régression ajouté (`test_render_html_escapes_finding_content_from_scanned_targets`).
- **Injection de balisage trouvée et corrigée dans les rapports PDF** : `pdf_renderer.py` interpolait le même type de contenu directement dans le balisage XML-like de reportlab (`<font>`, `<b>`) sans échappement — un contenu malformé pouvait casser la génération du PDF, ou un contenu conçu à cet effet pouvait usurper la mise en forme d'un rapport (ex. masquer du texte avec `<font color="white">`). Corrigé avec `xml.sax.saxutils.escape` sur toutes les valeurs issues des données ; test de non-régression ajouté.
- **Audit de dépendances** (`pip-audit`, `npm audit`) :
  - `npm audit` (frontend) : 0 vulnérabilité.
  - `pip-audit` (backend) : 11 CVE sur 3 paquets. `python-dotenv` (1.0.1→1.2.2) et `jinja2` (3.1.5→3.1.6) mis à jour (bumps mineurs, sans risque). `starlette` (0.41.3, 6 CVE) nécessite de passer à une version majeure ultérieure de FastAPI incompatible avec la contrainte actuelle (`fastapi==0.115.6` épingle `starlette<0.42`) — **non corrigé dans cette passe**, mise à niveau différée pour éviter une bascule majeure non testée en fin de session ; à traiter dans un futur cycle avec la suite de tests complète comme filet de sécurité.
- **Revue de code** : recherche systématique de `shell=True`, `os.system`/`os.popen`, `eval`/`exec`, `pickle`, SQL par f-string — aucune occurrence dans `backend/`, `kali/agent/`, `labmanager/`.
- **Revue CORS** : `allow_origins` n'est jamais `"*"` (limité à `CORS_ORIGINS`, par défaut l'origine du frontend uniquement) ; combiné à `allow_credentials=True`, un wildcard serait de toute façon rejeté par les navigateurs — configuration actuelle saine.
- **Revue des conteneurs** : `USER` non-root partout, y compris `cyberlab-labmanager` depuis la bascule vers le Docker Socket Proxy (Phase 11, voir plus haut) ; seul `cyberlab-docker-proxy` a accès à `docker.sock` (lecture seule), et aucun service applicatif n'a `privileged: true`.

### Connu, accepté ou différé

- **`starlette` 0.41.3** : 6 CVE non corrigées (nécessite une bascule majeure de FastAPI). Voir ci-dessus.
- **`cyberlab-docker-proxy` garde un accès complet en lecture au socket réel** : la restriction porte sur la surface d'API exposée (pas d'`EXEC`/`VOLUMES`/`SYSTEM`/`SECRETS`/`SWARM`), pas sur un sandboxing des opérations `CONTAINERS`/`NETWORKS`/`IMAGES` elles-mêmes — voir la section Lab Manager pour le détail du risque résiduel accepté.
- **Pas de rate limiting** sur l'API — un acteur avec accès réseau à l'API (donc déjà authentifié si `AUTH_ENABLED=true`, ou sur `localhost` sinon) pourrait lancer un grand nombre de jobs/scans en boucle. Non traité dans cette passe ; à ajouter (ex. `slowapi`) avant toute exposition multi-utilisateurs.
- **Annulation d'un job `RUNNING`** reste best-effort côté processus distant (voir [api.md](api.md)).
- **Markdown** : le rendu Markdown n'échappe pas le HTML embarqué (le Markdown standard autorise le HTML en passthrough) — risque uniquement si le Markdown généré est lui-même repassé dans un moteur Markdown→HTML non assainissant en aval ; non modifié pour ne pas casser la syntaxe Markdown légitime, mais documenté ici comme angle mort connu.

## RBAC (ADMIN/ANALYST/USER) — délibérément non implémenté

Évalué en Phase 11 (spec section 17 : « si faisable sans refonte majeure »). Conclusion : **pas faisable sans refonte majeure**, donc non implémenté, par choix plutôt que par oubli.

- **Il n'existe aujourd'hui aucune identité utilisateur.** `AUTH_ENABLED=true` protège l'API avec un unique secret partagé (`API_SECRET_KEY`, voir « Authentification API » ci-dessus) — quiconque possède le token a un accès identique à tout `/api/*`. Il n'y a ni table `users`, ni login, ni session, ni notion de « qui » fait la requête au-delà de « quelqu'un avec le bon token ».
- **Un rôle sans identité n'a pas de sens** : attribuer `ADMIN`/`ANALYST`/`USER` à une requête suppose de savoir qui l'a émise. Faire cela correctement demanderait un modèle `User`, un mécanisme de login (et donc de stockage de mots de passe, de hashing, potentiellement de sessions/JWT), une migration de authentification par token unique vers une authentification par compte — exactement le type de « refonte majeure » que la spec elle-même demande d'éviter si le RBAC n'est pas faisable sans cela.
- **Une fausse RBAC serait pire que pas de RBAC** : ajouter un champ `role` cosmétique sur un modèle inexistant, ou un rôle statique lu depuis une variable d'environnement partagée par tous les appelants du même token, donnerait une illusion de séparation de privilèges sans en fournir la garantie réelle — un risque de sécurité en soi (faux sentiment de confiance).
- **Ce qui existe déjà et couvre une partie du besoin sans compte utilisateur** :
  - Le **Policy Engine côté Job Engine** (`POST /api/jobs`, voir section IA ci-dessus et [ai.md](ai.md)) joue déjà un rôle d'autorisation *par action*, indépendamment de qui appelle : un `target_id` non `LAB`/`AUTHORIZED`/`LOCAL` est rejeté (403) quel que soit le porteur du token.
  - Le champ `authorization_status` des Targets (`backend/app/targets/authorization.py`) est la véritable frontière de sécurité de l'outil aujourd'hui — pas un rôle utilisateur, mais une propriété de la ressource ciblée, non modifiable par l'IA (voir ci-dessus).
- **Chemin recommandé si le multi-utilisateur devient un besoin réel** : introduire un modèle `User` minimal (email/hash de mot de passe ou intégration OAuth), un login émettant un JWT porteur d'un rôle, remplacer le token partagé unique par cette authentification par compte, puis ajouter un `Depends(require_role(...))` sur les routes sensibles (ex. suppression de Project/Target réservée à `ADMIN`). Non entamé ici pour rester fidèle à la contrainte « ne pas sur-ingénierer ».

## Phase 11 — Audit de sécurité final

| Catégorie | Statut | Justification |
|---|---|---|
| Command Injection | **PASS** | `shell=False` partout, arguments en liste jamais concaténés (`kali/agent/main.py`), allowlist stricte d'exécutables résolue par `shutil.which` au démarrage. Aucune occurrence de `shell=True`/`os.system`/`os.popen` dans `backend/`, `kali/agent/`, `labmanager/` (revue de code Phase 10, revérifiée). |
| Shell Injection | **PASS** | Défense en profondeur au-delà de `shell=False` : rejet explicite des métacaractères shell et du pattern d'injection de flag côté Tool Registry (`backend/app/tools/registry.py`, `test_build_command_rejects_shell_metacharacters`, `test_build_command_rejects_flag_injection_via_target`). |
| Path Traversal | **PASS** | Aucun chemin fichier n'est jamais construit à partir d'une entrée utilisateur : les rapports sont stockés en base (colonne `content`, pas de fichier), `report_id` est un `uuid.UUID` typé par FastAPI (422 avant même d'atteindre la route sur une valeur non-UUID) — voir `backend/app/api/routes/reports.py`. |
| SSRF | **PASS** (portée limitée) | Les seuls appels HTTP sortants du backend (`app/ai/ollama.py`, `app/jobs/kali_client.py`, `app/api/routes/labs.py`, `app/api/routes/health.py`) ciblent des URLs fixes de configuration (`ollama_url`, `kali_agent_url`, `labmanager_url`), jamais une URL fournie par l'appelant. Le seul "accès réseau piloté par l'utilisateur" est le scan d'une cible via le Tool Registry — c'est la fonction voulue de l'outil, exécutée dans le conteneur Kali isolé (réseau dédié, `cap_drop: ALL`, non-root) et bornée par `authorization_status` pour le chemin `target_id`. |
| XSS | **PASS** | Aucun `v-html` dans le frontend (interpolation Vue `{{ }}` échappée par défaut) ; rendu HTML des rapports avec `autoescape=True` et rendu PDF avec échappement `xml.sax.saxutils.escape` (trouvés et corrigés Phase 10, tests de non-régression en place). |
| SQL Injection | **PASS** | ORM SQLAlchemy 2.0 (`select(...)`, `AsyncSession`) partout, aucune requête construite par f-string/concaténation (revue de code Phase 10, revérifiée sur `app/api/routes/projects.py` et `targets.py` ajoutés en Phase 11). |
| WebSocket Authentication | **PASS** | `/api/ws/jobs/{id}` et `/api/ws/terminal` passent tous deux par le même middleware ASGI que le HTTP ; token accepté en `?token=` (les navigateurs ne peuvent pas fixer d'en-tête sur un handshake WS) ; testé explicitement pour les deux endpoints (`backend/tests/test_auth_middleware.py`), y compris le test ajouté en Phase 11 pour `/api/ws/terminal` qui n'avait pas de couverture dédiée. |
| Authentication Bypass | **PASS** | Middleware ASGI pur (pas `BaseHTTPMiddleware`) monté avant le routing, garde tout `/api/*` sauf `/api/health`. Nouveau test paramétré Phase 11 (`test_every_api_route_is_guarded_when_auth_enabled`) vérifie explicitement les 11 routers montés — y compris les endpoints "oubliables" comme `/api/reports/{id}/download`, `/api/labs/definitions`, `/api/ai/plan` — pour éviter qu'un futur router ajouté échappe silencieusement à la protection. |
| Authorization Bypass (Target) | **PARTIAL — PASS pour le chemin `target_id`, gap connu et documenté pour le chemin texte libre** | `POST /api/jobs` avec `target_id` est le seul point d'application (`is_executable()`, 403 si `UNKNOWN`) — c'est le chemin qu'empruntent systématiquement l'IA (target_id toujours injecté côté serveur, jamais par le modèle) et la nouvelle UI Targets. **Mais** `POST /api/jobs` accepte aussi un `target` en texte libre sans `target_id`, hérité tel quel des Phases 3–9 (page Tools classique) : ce chemin n'a jamais de `Target` en base à vérifier, donc aucune notion d'autorisation ne s'y applique — comportement inchangé depuis avant la Phase 11, préservé intentionnellement (contrainte « ne jamais casser l'existant »). Un attaquant/utilisateur qui connaît une adresse peut donc toujours lancer un scan dessus via la page Tools sans jamais créer de Target — l'autorisation borne les flux passant par le modèle Target (IA, UI Targets), pas l'usage direct historique de l'outil. Documenté ici plutôt que corrigé silencieusement ; option de durcissement future : déprécier le chemin texte libre ou exiger `target_id` pour tout nouveau job. |
| Docker Escape Surface | **DEFERRED** | Mitigations en place pour tous les conteneurs qui touchent du contenu potentiellement hostile (`cyberlab-kali`, conteneurs de labs) : non-root, `cap_drop: ALL`, pas de `privileged: true`, pas de `docker.sock`. Aucun test d'échappement de conteneur actif (ex. exploitation d'une CVE du runtime) n'a été mené — hors de portée d'un audit applicatif ; dépend de la version du moteur Docker de l'hôte, pas du code de CyberLab. |
| docker.sock | **PASS** | Isolé derrière `cyberlab-docker-proxy` (Docker Socket Proxy) depuis la Phase 11 — `cyberlab-labmanager` n'a plus d'accès direct au socket, tourne maintenant en non-root, et le proxy n'expose que `CONTAINERS`/`NETWORKS`/`IMAGES`/`POST` (pas d'`EXEC`/`VOLUMES`/`SYSTEM`/`SECRETS`/`SWARM`). Cycle de vie complet d'un lab (create/start/stop/delete) revérifié en conditions réelles à travers le proxy. Voir section Lab Manager pour le risque résiduel accepté. |
| PTY Isolation | **PASS** | Le PTY n'est jamais ouvert par `cyberlab-api`, seulement par l'agent Kali à l'intérieur du conteneur durci ; testé (token manquant/incorrect → `close(4401)`), bug de process zombie corrigé en Phase 6. |
| AI Prompt Injection | **PASS** (par conception architecturale) | L'IA ne peut jamais agir directement — elle ne fait que produire du texte/JSON consommé par l'utilisateur ou par un parseur strict (`step.tool` mis à `None` si le nom n'est pas dans le Tool Registry). Testé de façon adversariale (`backend/tests/ai/test_ai_security_boundary.py`) avec un faux provider qui prétend avoir modifié la base ou exécuté un `rm -rf /` : la base de données reste inchangée dans tous les cas, car `app/ai/` n'a structurellement aucun accès en écriture à `Target` (vérifié par un test statique, `test_ai_module_has_no_write_access_to_target_model`). |
| AI Tool Abuse | **PASS** | Un outil halluciné (`bash`, `metasploit`) est toujours filtré à `None` avant retour au frontend (`test_ai_planner_never_proposes_unregistered_tool_as_executable`) ; même si le frontend transmettait quand même un nom d'outil arbitraire, `POST /api/jobs` revalide via `registry.get_tool()` (404) et `registry.build_command()` (400) indépendamment de la provenance de la requête. |
| Report Generation | **PASS** | XSS (HTML) et injection de balisage (PDF) trouvées et corrigées en Phase 10, tests de non-régression en place ; contenu de rapport dérivé exclusivement de données déjà validées (findings/jobs en base), jamais de template utilisateur arbitraire. |
| File Downloads | **PASS** | Voir Path Traversal ci-dessus — pas de lecture de fichier basée sur une entrée utilisateur ; le nom de fichier de téléchargement est généré côté serveur à partir de l'UUID du rapport, jamais reflété depuis une entrée utilisateur. |
| Secrets | **PASS** | `.env` dans `.gitignore`, aucun secret en dur dans le code, tous les tokens (`API_SECRET_KEY`, `KALI_AGENT_TOKEN`, `LABMANAGER_TOKEN`, `POSTGRES_PASSWORD`) générés aléatoirement en environnement de dev local. |
| CORS | **PASS** | `allow_origins` n'est jamais `"*"` (limité à `CORS_ORIGINS`, l'origine du frontend par défaut) ; combiné à `allow_credentials=True`, un wildcard serait de toute façon refusé par les navigateurs. |
| Rate Limiting | **DEFERRED** | Aucun rate limiting sur l'API — acceptable pour un outil local mono-utilisateur derrière `127.0.0.1` (ou un token partagé si exposé), mais à ajouter (ex. `slowapi`) avant toute exposition réseau multi-utilisateurs. Décision déjà actée en Phase 10, reconfirmée ici sans changement. |

**Aucune vulnérabilité n'a été trouvée pendant cet audit qui nécessite un cycle REPRODUCE → DOCUMENT → FIX → TEST → RETEST** (contrairement aux deux trouvées et corrigées en Phase 10 — XSS HTML et injection PDF, toujours actives et testées). Le seul point notable est le gap *Authorization Bypass* ci-dessus, qui est un choix de conception documenté (préservation du chemin texte libre historique) plutôt qu'un bug — aucun test existant n'a été supprimé ou affaibli pour "faire passer" cet audit.

## Phase 12 — Expansion du Tool Registry (31 outils) : audit et bugs réels trouvés

### Surface ajoutée

- **`CAP_NET_RAW`** ajouté à `cyberlab-kali` (voir section « Exécution des outils » plus haut) — la seule relaxation de `cap_drop: ALL` dans toute la stack, nécessaire pour `masscan`/`traceroute`. Risque résiduel : le conteneur peut désormais fabriquer des paquets bruts (spoofing d'adresse source inclus) — mais uniquement vers `cyberlab-kali-net` et les réseaux de labs dynamiquement attachés, jamais vers le réseau hôte ou Internet au-delà de ce que le NAT Docker autoriserait de toute façon.
- **`nuclei` contacte Internet au premier lancement** (mise à jour de ses templates, comportement natif de l'outil, pas une fonctionnalité CyberLab) — le seul outil de l'arsenal à le faire. Le conteneur `cyberlab-kali` a un accès sortant Internet non restreint par défaut (bridge Docker standard, aucun pare-feu de sortie configuré) ; ceci était déjà vrai avant la Phase 12 mais n'était exploité par aucun outil du MVP à 3 outils. Accepté comme comportement natif documenté plutôt que bloqué, faute de mécanisme de pare-feu de sortie dans cette phase — piste de durcissement future si nécessaire.
- **`sqlmap` — `MANUAL_ONLY`, `ai_allowed: false`, surface volontairement bornée** : seuls la détection et l'énumération de noms de bases sont exposées (`--dbs`) ; `--dump`, `--os-shell`, `--sql-shell` (exfiltration de données / exécution de commande arbitraire) ne sont **jamais déclarés** comme arguments dans `sqlmap.yaml`, donc structurellement inatteignables via cette API, quel que soit l'appelant. `risk_level: MANUAL_ONLY` impose `ai_allowed: false` au niveau du schéma pydantic (`ToolDefinition._manual_only_implies_not_ai_allowed`) — impossible de committer une définition qui viole cette contrainte sans faire échouer le chargement du registre au démarrage.
- **`ai_allowed` appliqué à deux niveaux** (filtrage du prompt + revalidation de la réponse) — voir section IA plus haut pour le détail, testé de façon adversariale avec `sqlmap` (un vrai nom d'outil, pas une hallucination).
- **Tool Health (`GET /api/tools/health`)** : vérification non destructive (`--version`/`--help`, jamais un scan réel), mais limite assumée — un `--version` qui réussit prouve seulement que le binaire se lance, pas que sa fonction sous les privilèges actuels du conteneur fonctionne (ex. `arp-scan` répond `ready` même si un scan réel échouerait faute de capability suffisante). Documenté dans [tools.md](tools.md) plutôt que présenté comme une garantie plus forte qu'elle ne l'est.

### Bugs réels trouvés et corrigés (tests de bout en bout contre une vraie cible externe, `scanme.nmap.org`)

Contrairement aux phases précédentes qui testaient surtout contre le lab DVWA local, la Phase 12 a testé contre une cible Internet réelle — ce qui a révélé plusieurs bugs invisibles jusque-là :

- **`whatweb` pouvait sortir en exit 1 alors que le scan avait réussi** : sans `-q`, le logger console par défaut de whatweb et le logger `--log-json=-` écrivent tous deux sur stdout ; à la fermeture, une race entre les deux fait planter whatweb avec `closed stream (IOError)` (Ruby) — le JSON produit est pourtant complet et correct, mais le job était marqué `FAILED` (exit_code≠0) et n'extrayait donc aucun finding. Corrigé en ajoutant `-q` à `fixed_args`. Reproduit de façon fiable (100% des essais), corrigé, retesté avec succès.
- **`gobuster`/`ffuf` référençaient un chemin de wordlist inexistant** : `/usr/share/wordlists/dirb/*.txt` n'existe pas — le paquet `dirb` installe ses wordlists dans `/usr/share/dirb/wordlists/*.txt` (chemin inversé). Corrigé dans les deux définitions YAML.
- **`nikto` pouvait rester bloqué jusqu'au timeout sans jamais scanner** : par défaut, nikto tente de vérifier une mise à jour de plugins auprès d'un service distant à chaque lancement ; cette requête peut être rejetée (403 observé) puis nikto reste bloqué jusqu'au timeout du job au lieu de continuer. Corrigé avec `-nocheck` (`fixed_args`). Le `default_timeout` du profil `basic_web_scan` a aussi été relevé de 120s à 280s — un scan nikto complet contre une vraie cible Internet (latence réseau réelle) prend légitimement plus longtemps que contre un lab local ; ce n'est pas un bug mais une caractéristique du scan à checks complets, désormais reflétée dans le budget de timeout plutôt qu'ignorée.
- **Crash de l'agent Kali sur timeout (`TypeError: can't concat str to bytes`)** : `subprocess.TimeoutExpired.stdout`/`.stderr` peuvent revenir en `bytes` même avec `text=True` passé à `subprocess.run` (capturés au moment du kill, avant le décodage normal) ; le handler faisait `(exc.stderr or "") + "\n[cyberlab] ..."`, un plantage `bytes + str` qui retournait un `500 Internal Server Error` opaque au lieu d'un rapport de timeout propre — masquant la vraie cause de l'échec (job `FAILED` avec `error: "kali agent rejected request: Internal Server Error"`, sans aucun détail exploitable). Trouvé en scannant nikto contre une cible réelle. Corrigé avec une coercion défensive `bytes → str` ; test de régression ajouté (`kali/agent/tests/test_exec_timeout.py`), qui simule explicitement le cas `bytes` pour empêcher toute régression future.

Les quatre corrections ont suivi REPRODUCE → DOCUMENT → FIX → TEST → RETEST : chaque bug a été reproduit de façon déterministe avant modification, puis la commande/le job a été rejoué avec succès après correction.

## Phase 13 — Asset Model : audit de sécurité

Surface ajoutée : nouveau routeur `/api/assets` (+ `POST /api/projects/{id}/assets`), généralisation du modèle `Target` en `Asset` (même table renommée, voir [phase-13-asset-model.md](phase-13-asset-model.md)). Aucun changement au Policy Engine, à l'agent Kali, au Lab Manager ou à l'IA.

| Catégorie | Statut | Justification |
|---|---|---|
| Authorization Bypass (Asset) | **PASS — inchangé** | `is_executable()`/`infer_default_authorization()` (`backend/app/targets/authorization.py`) non modifiés. `POST /api/jobs` reste l'unique point d'application, désormais sur des lignes `Asset`. Le gap connu du chemin `target` texte libre (documenté Phase 11) est inchangé — `record_asset_activity()` ne s'exécute que si `job.target_id` est renseigné, donc ce chemin reste hors du système d'autorisation *et* hors du système d'activité, cohérent avec son statut déjà documenté. |
| Authentication Bypass | **PASS** | `/api/assets`, `/api/assets/{id}`, `/api/assets/{id}/jobs`, `/api/projects/{id}/assets` ajoutés explicitement à `test_every_api_route_is_guarded_when_auth_enabled` (`backend/tests/test_auth_middleware.py`) — vérifié, pas seulement supposé par la garde de préfixe de chemin. |
| Mass Assignment | **PASS** | `AssetUpdateRequest` n'expose pas `first_seen`/`last_seen`/`technologies` — un `PATCH` tentant de les fixer est silencieusement ignoré (Pydantic ignore les clés absentes du schéma), testé (`test_update_asset_cannot_set_first_seen_directly`). Ces champs ne sont écrits que par `app/assets/activity.py`, appelé uniquement depuis `execute_job()` côté worker, jamais depuis une route API. |
| SQL Injection | **PASS** | Filtre `search` sur `Asset.name` via `ilike()` lié par paramètre SQLAlchemy, même pattern que `targets.py` — aucune concaténation de chaîne. |
| XSS | **PASS** | Tags/technologies (chaînes utilisateur ou dérivées d'un outil comme whatweb) rendus via interpolation Vue `{{ }}` (échappement par défaut), aucun `v-html` ajouté dans `targets/index.vue`/`targets/[id].vue`. |
| AI Boundary | **PASS — inchangé** | Aucun fichier sous `app/ai/` n'importe `Asset`/`Target` — `test_ai_module_has_no_write_access_to_target_model` (scan statique de `app/ai/*.py`) passe sans modification ; l'IA n'a acquis aucun accès supplémentaire via cette phase. |
| Migration Safety | **PASS** | Renommage de table/colonnes/enum, jamais de `DROP`/perte de données ; backup `pg_dump` pris avant application sur la base dev réelle ; `downgrade()` fourni (limite documentée : les valeurs d'enum ajoutées par `ADD VALUE` ne peuvent pas être retirées proprement par Postgres, restent inutilisées après un rollback). |
| API Compatibility | **PASS** | `/api/targets`, `/api/projects/{id}/targets`, `TargetResponse` inchangés — 152 tests (dont tous les tests Phase 11 `tests/targets/`, `tests/projects/`) verts sans modification de leur code, uniquement du modèle sous-jacent. |

Aucune vulnérabilité trouvée nécessitant un cycle REPRODUCE → DOCUMENT → FIX → TEST → RETEST pour cette phase.

## Phase 14 — Continuous Recon + Diff Engine : audit de sécurité

Surface ajoutée : `ScheduledJob` (planification), le ticker (`app/scheduling/ticker.py`, thread dans `cyberlab-worker`), `AssetChangeEvent`/Diff Engine (`app/diff/`), routeur `/api/schedules` + `/api/assets/{id}/schedules` + `/api/assets/{id}/changes`. Voir [phase-14-continuous-recon.md](phase-14-continuous-recon.md) pour l'architecture complète.

| Catégorie | Statut | Justification |
|---|---|---|
| Policy Engine Bypass | **PASS** | Un `ScheduledJob` ne déclenche jamais son propre chemin d'exécution : `app/jobs/service.py::prepare_job()` (validation Tool Registry) et `app.targets.authorization.is_executable()` (autorisation) — les deux mêmes fonctions que `POST /api/jobs` — sont appelés à la création du schedule, à chaque tick automatique, et à chaque "Run now" manuel. Aucune duplication de logique d'autorisation, donc aucun risque de divergence future entre le chemin manuel et le chemin planifié. |
| Asset Authorization Bypass | **PASS** | Testé explicitement (`test_asset_becomes_unauthorized_refuses_run_but_stays_active`) : un asset dont l'autorisation passe à `UNKNOWN` après création du schedule voit son exécution automatique refusée (aucun Job créé, `consecutive_failures` incrémenté), pas de fenêtre où l'ancienne autorisation resterait utilisable. |
| Tool Restriction Bypass | **PASS** | Un tool retiré du registre (ou un profil invalide) après création du schedule est re-détecté à chaque tick via `registry.get_tool()`/`registry.get_profile()` (mêmes exceptions que la validation manuelle) — testé (`test_tool_no_longer_valid_is_refused_without_crashing_ticker`). `ai_allowed` n'entre pas en jeu ici : un `ScheduledJob` est une action humaine (comme le Tools page), pas un chemin IA — cohérent avec le fait qu'un humain peut déjà exécuter manuellement un outil `MANUAL_ONLY`. |
| Job Duplication / Race Conditions | **PASS** | `FOR UPDATE SKIP LOCKED` + avancement de `next_run_at` avant tout traitement (réservation Postgres, pas de verrou Redis). Testé avec deux vraies sessions Postgres concurrentes (`test_concurrent_claim_skip_locked_prevents_double_claim`) et avec deux tours de ticker consécutifs (`test_claiming_twice_immediately_does_not_double_claim`) — aucun double Job pour une même échéance dans les deux cas. |
| Scheduler Abuse (cron abuse) | **PASS** | `MIN_INTERVAL_SECONDS = 300` appliqué au niveau du schéma Pydantic (`ScheduledJobCreateRequest`/`UpdateRequest`), testé (`test_create_schedule_rejects_interval_below_minimum`) — impossible de créer un schedule qui martèle un asset plus vite qu'une fois toutes les 5 minutes. |
| Mass Assignment | **PASS** | `ScheduledJobUpdateRequest` n'expose ni `asset_id`/`project_id`/`next_run_at`/`last_run_at`/`last_job_id`/`consecutive_failures` — seuls `profile`/`params`/`interval_seconds`/`status` sont modifiables via l'API ; les champs dérivés ne sont écrits que par le ticker/les routes d'exécution serveur. `AssetChangeEvent` n'a **aucune route de création/modification** — uniquement généré par `app/diff/service.py`, jamais accepté en entrée API. |
| Injection dans les résultats JSON / old_value-new_value | **PASS** | `old_value`/`new_value` (potentiellement dérivés de bannières de service, noms de plugins whatweb, sujets de certificat — donc de données de la cible scannée, pas fiables) sont toujours convertis en `str()` avant stockage (`app/diff/engine.py::_change()`), stockés en colonnes `Text` via l'ORM (jamais de SQL construit à partir de leur contenu), et rendus côté frontend via interpolation Vue `{{ }}` échappée par défaut — aucun `v-html`, aucune exécution. Un nom de plugin whatweb contenant `<script>` s'affiche comme texte littéral, ne s'exécute jamais. |
| SQL Injection | **PASS** | Toutes les requêtes du ticker et des routes passent par l'ORM SQLAlchemy (`select()`, `with_for_update()`) — aucune concaténation de chaîne, y compris pour les filtres `change_type`/`severity`/`before` de `GET /api/assets/{id}/changes`. |
| XSS | **PASS** | Voir "Injection dans les résultats JSON" ci-dessus — même garantie pour les tags/technologies déjà en place depuis la Phase 13. |
| Command Injection | **PASS — inchangé** | Un `ScheduledJob.params` passe par exactement la même validation `registry.build_command()` (anti flag-injection, anti métacaractères shell, allowlist d'arguments) qu'un job manuel — aucun nouveau chemin vers `subprocess`/l'agent Kali. |
| SSRF | **PASS — sans changement** | Le ticker n'effectue aucun appel HTTP sortant lui-même ; il enfile un job Redis/RQ (`queue.enqueue`), exactement comme `POST /api/jobs` — même surface réseau qu'avant cette phase. |
| Timezone Manipulation | **PASS** | `next_run_at` n'est jamais un champ accepté en entrée API (absent de `ScheduledJobCreateRequest`/`UpdateRequest`) — toujours calculé côté serveur (`datetime.now(timezone.utc)`), colonnes `DateTime(timezone=True)` partout. Aucune façon pour un appelant de forcer une échéance arbitraire dans le passé pour déclencher une exécution immédiate en boucle. |
| Authentication Bypass | **PASS** | `/api/assets/{id}/schedules`, `/api/assets/{id}/changes`, `/api/schedules/{id}` ajoutés explicitement à `test_every_api_route_is_guarded_when_auth_enabled`. |
| Asset Deletion Cleanup | **PASS** | `DELETE /api/assets/{id}` et `DELETE /api/targets/{id}` (même table depuis la Phase 13) désactivent les `ScheduledJob` associés dans la même transaction que la suppression (`disable_schedules_for_asset`) — testé pour les deux routes (`test_deleting_asset_disables_its_schedules`, `test_deleting_target_disables_its_schedules_too`). |
| AI Boundary | **PASS — inchangé** | Aucun fichier de `app/scheduling/`, `app/diff/`, ou `app/jobs/service.py` n'est importé par `app/ai/` ; le test statique `test_ai_module_has_no_write_access_to_target_model` passe sans modification. L'IA n'a aucun moyen de créer, modifier ou déclencher un `ScheduledJob`. |

**Point d'attention documenté plutôt que corrigé** : le thread ticker vit dans `cyberlab-worker`, qui garde un accès complet à la base Postgres (comme avant cette phase) — ce n'est pas un nouvel élargissement de surface, `execute_job()` avait déjà cet accès. Aucune vulnérabilité nouvelle trouvée nécessitant un cycle REPRODUCE → DOCUMENT → FIX → TEST → RETEST pour cette phase.

## Phase 15 — Risk Intelligence & Risk Score : audit de sécurité

Surface ajoutée : `app/intel/` (clients EPSS/CISA KEV/NVD + orchestration de sync), `app/risk/` (calculateur + service de recalcul), nouvelles colonnes sur `Finding`, routeur `/api/intelligence`, extension de `GET /api/findings`. Trois sources de données **externes non fiables** (FIRST.org, CISA, NVD) entrent dans le système pour la première fois.

| Catégorie | Statut | Justification |
|---|---|---|
| SSRF | **PASS** | Les trois clients (`app/intel/epss.py`, `cisa_kev.py`, `nvd.py`) appellent des URLs **fixes, codées en dur**, jamais construites à partir d'une entrée utilisateur — aucun paramètre de requête ne provient d'un CVE fourni par un appelant externe sans validation (les CVE proviennent uniquement de `Finding.cve_ids`, eux-mêmes extraits d'une sortie d'outil structurée). Aucun mécanisme n'accepte une URL arbitraire à contacter. |
| Untrusted External Data | **PASS** | Les trois sources sont traitées comme non fiables par construction : chaque champ est extrait avec `.get()`/valeurs par défaut (jamais un accès direct qui lèverait sur une clé manquante), converti explicitement (`float()`, `date.fromisoformat()` avec capture d'exception), et une entrée malformée individuelle est ignorée sans faire échouer tout le lot (`fetch_epss_scores` : un CVE avec un `epss` non numérique est simplement absent du résultat, testé). Aucune donnée externe n'est jamais interpolée dans une requête SQL, un chemin de fichier, ou une commande. |
| DoS via réponse volumineuse | **PASS** | CISA KEV (~1,5 Mo, catalogue borné, pas un flux illimité) est le seul téléchargement complet ; EPSS est requêté par lots de 100 CVE (`BATCH_SIZE`) parmi les CVE réellement rencontrés (jamais des dizaines de milliers) ; NVD est plafonné à `NVD_MAX_CVES_PER_RUN = 20` par cycle. Timeouts explicites sur les trois clients (15–30s). |
| Timeout / Retry Storm | **PASS** | Chaque appel HTTP a un timeout explicite (`httpx.Client(timeout=...)`) ; un échec (`EPSSFetchError`/`CisaKevFetchError`/`NVDFetchError`) est catché et enregistré dans `IntelSyncState.last_error` **sans retry en boucle serrée** — le prochain essai attend le prochain cycle (24h) ou un déclenchement manuel explicite. NVD respecte son rate-limit non authentifié via un délai fixe entre appels (`NVD_MIN_INTERVAL_SECONDS`), pas une boucle de retry agressive. |
| Cache Poisoning | **PASS (risque résiduel documenté)** | `vulnerability_intel`/`cisa_kev_entries` ne sont modifiables que par le thread de sync (jamais par une route API en écriture) — aucun endpoint n'accepte de CVSS/EPSS/KEV en entrée. Risque résiduel accepté et documenté : CyberLab fait confiance à l'intégrité TLS + au contenu servi par `api.first.org`/`cisa.gov`/`nvd.nist.gov` eux-mêmes (comme toute intégration à une source de threat intel publique) — hors du contrôle applicatif de CyberLab, cohérent avec la confiance déjà accordée à nuclei's propre catalogue de templates (Phase 12). |
| Score Manipulation | **PASS** | `Finding.risk_score`/`risk_priority`/`cvss_score`/`epss_score`/`kev` sont absents de tout schéma de requête API en écriture (`FindingResponse` est en lecture seule, aucune route `PATCH /api/findings/{id}` n'existe) — uniquement `app/risk/service.py` peut les écrire, appelé uniquement depuis le worker/la sync/le déclencheur de criticité. Impossible pour un appelant API de forcer un score arbitraire. |
| Modification non autorisée de Criticality/Confidence | **PASS — inchangé** | `Asset.criticality` reste modifiable uniquement via `PATCH /api/assets/{id}` (Phase 13, action humaine explicite, authentifiée) ; `Finding.confidence` n'a toujours aucune route d'écriture (fixé à l'extraction, Phase 9). Le Risk Score consomme ces valeurs, ne leur ouvre aucun nouveau chemin de modification. |
| Injection dans les explications / breakdown | **PASS** | `explanation`/`components` sont générés exclusivement par `app/risk/calculator.py` à partir de gabarits Python (f-strings sur des nombres/enums), jamais à partir de texte libre externe. Le seul texte dérivé d'une source externe visible dans l'UI Risk (`cvss_version`, une chaîne courte comme `"3.1"`) est rendu via interpolation Vue échappée — pas de `v-html`. |
| Mass Assignment | **PASS** | `AssetUpdateRequest` (Phase 13, inchangé) ne s'est pas élargi ; aucun nouveau schéma d'écriture n'a été ajouté pour les champs de risque. |
| IDOR / Authorization | **PASS** | `GET /api/findings/{id}/risk` et `GET /api/assets/{id}/risk-summary` suivent le même modèle d'accès que le reste de l'application mono-utilisateur (pas de séparation par compte, cohérent avec l'absence de RBAC déjà documentée en Phase 11) — protégés par le même middleware d'authentification, vérifié explicitement (`test_every_api_route_is_guarded_when_auth_enabled`). |
| Policy Engine Bypass | **PASS — sans changement** | Aucun chemin de cette phase ne crée ni n'exécute de Job — `app/intel/sync.py` ne touche jamais `app/jobs/service.py`/`is_executable()`. La seule action utilisateur qui déclenche un Job reste `POST /api/jobs` (Phase 11), inchangé. |

**Bug réel trouvé et corrigé pendant la vérification E2E** (pas une faille de sécurité, mais documenté selon le même principe REPRODUCE → DOCUMENT → FIX → TEST → RETEST que les phases précédentes) : le profil par défaut de nuclei (`severity: "info,low,medium"`, Phase 12) filtre silencieusement un scan `-tags` dont l'unique template correspondant est `critical`, produisant `[FTL] Could not run nuclei: no templates provided for scan`. Comportement du Tool Registry existant, pas de cette phase — documenté ici car découvert pendant la vérification E2E du Risk Score plutôt que contourné silencieusement.

## Phase 16 — Corrélation, Déduplication & Cycle de vie : audit de sécurité

Surface ajoutée : `app/findings/{signature,service,lifecycle,correlation}.py`, tables `finding_status_history`/`finding_relations`, nouvelles colonnes sur `Finding` (`signature`/`status`/`first_seen`/`last_seen`/`observation_count`/`source_tools`/`observation_job_ids`), routes `GET /api/findings/{id}/history`, `GET /api/findings/{id}/relations`, `PATCH /api/findings/{id}/status`, filtres `status`/`source_tool` sur `GET /api/findings`. Voir [phase-16-correlation-deduplication.md](phase-16-correlation-deduplication.md) pour l'architecture complète.

| Catégorie | Statut | Justification |
|---|---|---|
| SQL Injection (titre/CVE/reason) | **PASS** | Toutes les requêtes passent par l'ORM SQLAlchemy (`select()`, `with_for_update()`, `session.add()`) — aucune concaténation de chaîne, y compris pour le champ libre `reason` de `PATCH /status`. Vérifié en conditions réelles contre le stack Docker : une chaîne `'; DROP TABLE findings; --` envoyée comme `reason` est stockée verbatim comme texte inerte (`finding_status_history.reason`, colonne `Text`), la table `findings` reste intacte et interrogeable après l'appel. |
| XSS (titre/description/reason) | **PASS** | `reason`/`old_status`/`new_status`/le `reason` d'une `FindingRelation` sont rendus côté frontend via interpolation Vue `{{ }}` échappée par défaut (`app/pages/findings/[id].vue`) — aucun `v-html` introduit par cette phase. Un `reason` contenant `<script>` s'affiche comme texte littéral. |
| IDOR sur Finding/relations/history | **PASS — cohérent avec le modèle existant** | Application mono-utilisateur sans RBAC (Phase 11) : les 3 nouvelles routes suivent le même modèle d'accès que le reste de l'API (pas de séparation par compte), protégées par le même middleware d'authentification — `test_every_api_route_is_guarded_when_auth_enabled` étendu à `/api/findings/{id}/history`, `/relations`, `/status`. |
| Mass Assignment | **PASS** | `FindingStatusUpdateRequest` n'expose que `status`/`reason` — `first_seen`/`last_seen`/`observation_count`/`source_tools`/`signature` sont absents de tout schéma d'écriture. Vérifié en conditions réelles : un `PATCH /status` avec `observation_count`/`signature`/`first_seen` injectés dans le corps de la requête voit ces champs silencieusement ignorés par Pydantic (`extra` non déclaré), aucun effet sur la ligne en base. `FindingResponse` (lecture) n'a jamais été le véhicule d'une écriture. |
| Création frauduleuse de relations | **PASS** | Aucune route `POST`/`PUT`/`PATCH` n'existe pour `FindingRelation` — uniquement `app/findings/correlation.py` peut en créer, à partir des 3 règles fixes, jamais depuis un corps de requête. |
| Policy Engine Bypass | **PASS — sans changement** | Aucun chemin de cette phase ne crée ni n'exécute de Job — la déduplication/corrélation se déclenche uniquement après un `Job` déjà `SUCCESS` (`execute_job()`), jamais avant ou à sa place. |
| UPSERT Race Condition | **PASS** | Prouvé avec deux vraies sessions PostgreSQL/threads Python déposant simultanément la même signature (`tests/findings/test_concurrency.py`) : exactement 1 Finding en résultat, rejoué 5 fois consécutives sans échec. Voir aussi le bug réel trouvé (index unique absent du modèle ORM, donc absent de la base de test) documenté dans [phase-16-correlation-deduplication.md](phase-16-correlation-deduplication.md). |
| Enum Injection | **PASS** | `FindingStatus` est un `Enum` Pydantic strict — une valeur hors énumération sur `PATCH /status` (ex. `"HACKED"`) est rejetée en `422` avant d'atteindre la logique métier, vérifié en conditions réelles. Les transitions elles-mêmes sont revalidées côté serveur (`is_valid_transition`) indépendamment de la validation de type — un statut syntaxiquement valide mais atteint par une transition interdite (ex. `NEW → REMEDIATED`) est rejeté en `400`. |
| JSON Injection (`evidence`/`relation_metadata`) | **PASS — inchangé** | Ni l'un ni l'autre n'est jamais accepté en entrée d'une route API en écriture — `evidence` provient exclusivement du pipeline d'extraction (Phase 9), `relation_metadata` exclusivement des règles de corrélation. Stockage `JSON` via l'ORM, pas de construction de requête à partir de leur contenu. |
| Confiance dans les données des outils (titre/description/évidence) | **PASS** | Cohérent avec la Phase 14 : `source_tool`/`title`/`evidence` proviennent de la sortie d'un outil scanné (donc potentiellement hostile si la cible est compromise) — jamais utilisés dans une requête SQL construite dynamiquement, un chemin de fichier, ou une commande. Le `reason` généré par une règle de corrélation interpole un titre de Finding tel quel (`f'... whatweb observed "{of.title}" ...'`) mais reste toujours rendu comme texte échappé côté frontend, jamais exécuté. |
| Auto-réouverture abusive (contournement de décision humaine) | **PASS** | `ACCEPTED_RISK` est explicitement exclu de `AUTO_REOPEN_FROM` (`app/findings/lifecycle.py`) — aucune re-observation, aussi répétée soit-elle, ne peut annuler une décision humaine explicite. Vérifié en conditions réelles (3 re-scans nmap consécutifs sur un Finding `ACCEPTED_RISK`, statut inchangé à chaque fois). |
| Auto-confirmation abusive | **PASS** | Aucune transition automatique vers `CONFIRMED` n'existe — `maybe_auto_reopen()` ne gère que `REMEDIATED`/`FALSE_POSITIVE → REOPENED`, jamais `NEW → CONFIRMED`. Une simple re-détection ne vaut jamais confirmation humaine. |

**Bugs réels trouvés et corrigés pendant la vérification** (pas des failles de sécurité au sens strict, sauf le second qui touche directement la garantie anti-duplication — documentés selon le principe REPRODUCE → DOCUMENT → FIX → TEST → RETEST) : voir la section dédiée dans [phase-16-correlation-deduplication.md](phase-16-correlation-deduplication.md#bugs-réels-trouvés-et-corrigés-pendant-la-vérification-reproduce--document--fix--test--retest) — plantage sur Finding sans Asset (flush manquant), **index unique de déduplication absent du modèle ORM** (présent uniquement dans la migration, donc invisible à `Base.metadata.create_all()` et à la suite de tests), objet déjà détaché lors d'un retry de concurrence, et extraction du `target` whatweb/nuclei empêchant toute corrélation par port en production.

## Phase 17 — Security Graph : audit de sécurité

Surface ajoutée : table `graph_edges`, `app/graph/{builder,service,queries}.py`, routeur `/api/graph` (5 routes : 4 lectures + 1 déclenchement de rebuild). Nouvelle capacité : traversal récursif multi-hop sur des données pouvant provenir de plusieurs Projects/Assets.

| Catégorie | Statut | Justification |
|---|---|---|
| IDOR — accès à un Asset d'un autre Project | **PASS — cohérent avec le modèle existant** | Application mono-utilisateur sans RBAC (Phase 11) : comme pour le reste de l'API, aucune notion de "propriétaire" de Project n'existe pour qu'un accès puisse être "non autorisé" entre deux Projects du même déploiement. `GET /api/graph/assets/{id}` fonctionne pour n'importe quel Asset existant, cohérent avec `GET /api/assets/{id}` (Phase 13) déjà sans restriction par Project. |
| Fuite de données entre Projects | **PASS** | La règle `RELATED_TO` Asset↔Asset est scopée par construction à `Asset.project_id == asset.project_id` (`app/graph/builder.py`) — deux Assets de Projects différents ne sont jamais reliés, donc un traversal ne peut jamais "sauter" d'un Project à un autre via cette règle. Vérifié explicitement (`test_build_graph_never_links_assets_across_different_projects`). `GET /api/graph/projects/{id}` scope ses Assets par `WHERE Asset.project_id == project_id` — vérifié qu'un Asset d'un autre Project n'apparaît jamais dans la réponse (`test_get_project_graph_never_leaks_assets_from_other_projects`). |
| SQL Injection dans les filtres/`node_id` | **PASS** | Le traversal récursif (`app/graph/queries.py::_TRAVERSAL_SQL`) utilise `sqlalchemy.text()` avec des paramètres liés (`:seed_type`, `:seed_id`, `:max_depth`) — jamais de f-string/concaténation dans le SQL. Vérifié en conditions réelles : un `node_id` contenant `'; DROP TABLE graph_edges; --` retourne un graphe vide (payload traité comme une chaîne inerte), la table reste intacte et interrogeable après l'appel (`test_get_node_graph_rejects_sql_injection_attempt_in_node_id`). |
| Profondeur négative / nulle | **PASS** | `Query(ge=1)` côté FastAPI rejette `depth=0`/`depth=-1` en `422` avant d'atteindre la logique métier ; `local_graph()` revalide indépendamment (`InvalidDepthError`) pour tout appelant non-HTTP. |
| Profondeur énorme / Graph Traversal DoS | **PASS** | `MAX_GRAPH_DEPTH = 3` appliqué à la fois par `Query(le=MAX_GRAPH_DEPTH)` (`422` immédiat) et par `local_graph()` elle-même — un appelant ne peut jamais forcer un traversal au-delà de 3 hops, quel que soit le point d'entrée (route dédiée ou générique `/nodes/{type}/{id}`). |
| Cycles récursifs (boucle infinie) | **PASS** | La CTE récursive maintient un tableau `visited` et refuse d'avancer vers un nœud déjà présent sur le chemin courant — un vrai cycle à 3 nœuds (`A → B → C → A`) termine en ~46ms au lieu de boucler (`test_local_graph_handles_a_real_3_node_cycle_without_looping`, vérifié contre PostgreSQL réel, pas simulé). |
| JSON Injection (`edge_metadata`) | **PASS** | Jamais accepté en entrée d'une route API en écriture — uniquement écrit par `app/graph/builder.py` à partir de données déjà validées ailleurs (evidence nmap, `Asset.technologies`, `FindingRelation.relation_metadata`). Stockage `JSON` via l'ORM. |
| XSS via node labels / `reason` / `source` | **PASS** | Tous rendus côté frontend via interpolation Vue `{{ }}` échappée par défaut (`SecurityGraph.vue`) et via les attributs `data()` de Cytoscape (jamais interprétés comme HTML) — aucun `v-html` introduit par cette phase. Un label de technologie ou un `reason` contenant `<script>` s'affiche comme texte littéral. |
| Mass Assignment | **PASS** | Aucune route n'accepte `from_type`/`from_id`/`to_type`/`to_id`/`relation`/`source`/`reason`/`edge_metadata` en entrée — `GraphRebuildRequest` n'expose que `project_id`/`asset_id` (des UUID validés par Pydantic, jamais un edge arbitraire). Tous les edges sont exclusivement écrits par `app/graph/builder.py`. |
| `POST /api/graph/rebuild` non authentifié | **PASS** | Comme toutes les routes `/api/graph/*`, protégée par le même middleware d'authentification que le reste de l'API — vérifié explicitement (`test_every_api_route_is_guarded_when_auth_enabled` étendu à `/api/graph/rebuild` et aux 4 routes de lecture). |
| Exécutions concurrentes inutiles de rebuild | **PASS** | `POST /rebuild` enfile via la queue RQ existante (même substrat que `POST /api/jobs`/l'intel sync Phase 15) plutôt que d'exécuter en ligne — le worker RQ par défaut traite un job à la fois, ce qui sérialise gratuitement des rebuilds concurrents sans verrou applicatif supplémentaire à maintenir. |
| Provenance fictive / edge non justifié | **PASS** | Chaque edge porte un `reason` humainement lisible généré par la règle qui l'a créé (jamais un texte générique/vide) — vérifié pour chacune des 5 règles (`tests/graph/test_builder.py`). Aucune route API n'accepte de créer un edge directement, donc aucun edge ne peut exister sans provenance traçable. |

## Phase 18 — Agents spécialisés (IA) : audit de sécurité

Surface ajoutée : `app/ai/orchestrator.py`, `app/ai/agents/{correlation,report}.py`, tables `missions`/`mission_steps`/`ai_correlation_suggestions`, routes `/api/ai/{missions,correlation-suggestions,reports/propose}/...`. Voir [phase-18-ai-agents.md](phase-18-ai-agents.md) pour l'architecture complète (niveaux d'autonomie réellement livrés, séquence de sécurité d'`advance_mission()`, isolation Job/Mission).

| Catégorie | Statut | Justification |
|---|---|---|
| Policy Engine Bypass — nouveau chemin d'exécution | **PASS** | `MissionOrchestrator` ne crée jamais de `Job` autrement qu'en appelant `is_executable()` puis `prepare_job()` — les deux mêmes fonctions pures que `POST /api/jobs` et le ticker Phase 14, jamais réimplémentées. `app/jobs/service.py` et `app/api/routes/jobs.py` ne sont pas modifiés par cette phase. Vérifié explicitement (`test_authorization_revoked_between_steps_skips_and_stops_mission`, `test_ai_cannot_run_job_against_unauthorized_target_via_plan` déjà existant et toujours vert). |
| Autorisation revérifiée seulement à l'approbation, pas à chaque étape | **PASS** | `advance_mission()` recharge l'`Asset` serveur et rappelle `is_executable()` avant **chaque** étape, jamais seulement une fois à `approve_mission()`. Une autorisation révoquée entre deux étapes passe l'étape courante à `SKIPPED` (jamais de `Job` créé) et arrête la Mission — vérifié en conditions réelles avec deux vraies sessions PostgreSQL (`test_authorization_revoked_between_steps_skips_and_stops_mission`). |
| `target_id` choisi ou modifié par le modèle | **PASS** | `create_mission()` ne reçoit jamais un `target_id` depuis la sortie du modèle — la route (`app/api/routes/ai.py::create_mission`) résout et charge l'`Asset` réel *avant* d'appeler l'orchestrateur, qui ne reçoit qu'un objet `Asset` déjà validé, jamais un identifiant à interpréter. `Mission.target_id` est immuable après création (aucune route ne l'expose en écriture). Vérifié (`test_create_mission_target_id_is_server_side_not_model_chosen`). |
| Double création de `Job` en cas d'appels concurrents à `advance_mission()` | **PASS** | Verrouillage bloquant `SELECT ... FOR UPDATE` (pas `SKIP LOCKED`, contrairement au ticker Phase 14 qui verrouille un lot de lignes indépendantes) — le second appelant attend, relit l'état déjà à jour (étape `QUEUED` ou Mission terminale) et devient un no-op. Prouvé avec deux vrais threads/deux vraies sessions PostgreSQL, l'un bloqué par construction (`Job.enqueue()` mocké pour tenir la transaction ouverte) pendant que l'autre est vérifié explicitement bloqué à son tour sur le verrou (`test_concurrent_advance_mission_creates_only_one_job`) : exactement un `Job` créé. |
| Isolation Job/Mission — un bug d'avancement de Mission corrompt le résultat d'un Job | **PASS** | Le hook `advance_mission_for_job()` s'exécute dans le `finally` le plus externe d'`execute_job()`, strictement après la fermeture de la session du Job et le commit de son statut terminal — dans sa **propre** session, avec son propre `try`/`except` qui journalise et avale toute erreur sans jamais pouvoir réécrire `job.status`. Vérifié en forçant `advance_mission_for_job` à lever une exception : le `Job` reste `SUCCESS`, exit_code et `finished_at` intacts (`test_job_stays_success_even_if_mission_advancement_fails`). |
| `MissionStep` prétendant un statut terminal sans `Job` réel | **PASS** | Contrainte DB (`ck_mission_step_job_id_required_when_executed`) : `status IN ('QUEUED','SUCCESS','FAILED')` exige `job_id IS NOT NULL`. Un outil halluciné par le modèle ou rejeté par `prepare_job()` (jamais tenté comme `Job`) passe donc en `SKIPPED`, pas `FAILED` — bug réel trouvé pendant la vérification (voir plus bas). |
| `max_steps` contourné par un plan plus long ou des lignes ajoutées après coup | **PASS** | Plafonné à la fois à la création (`create_mission()` tronque `plan.steps[:max_steps]`) et à chaque avancement (`advance_mission()` ignore toute étape `PENDING` avec `step_order >= mission.max_steps`) — défense en profondeur vérifiée explicitement en insérant une étape surnuméraire directement en base (`test_max_steps_cap_is_enforced_even_with_extra_pending_rows`). |
| Kill switch (`cancel_mission`) inefficace ou révocable | **PASS** | `CANCELLED` est un statut terminal : un appel `advance_mission()` ultérieur (y compris depuis le hook `execute_job()` d'un Job déjà en vol) redevient un no-op immédiat, jamais un nouveau `Job`. `cancel_mission()` ne touche jamais un `Job` déjà en file/en cours — ce reste la responsabilité de `POST /api/jobs/{id}/cancel` existant, jamais réinventée. Vérifié (`test_cancel_mission_prevents_future_steps`, `test_cancel_mission_does_not_touch_in_flight_job`). |
| `FindingRelation` créée directement par le Correlation Agent | **PASS** | `CorrelationAgent` (`app/ai/agents/correlation.py`) n'a aucun accès `Session` (vérifié statiquement, `test_correlation_and_report_agents_have_no_db_session_access`) — il ne peut physiquement pas écrire en base. Ses suggestions sont persistées par la route dans `AICorrelationSuggestion` (`status: PENDING`), une table dédiée, jamais dans `FindingRelation`. Vérifié en conditions réelles : après génération, `FindingRelation` ne contient aucune ligne pour la paire suggérée (`test_generate_correlation_suggestions_persists_pending`). |
| Suggestion IA devenant une relation sans validation humaine | **PASS** | Seule la route `POST /.../accept` crée une `FindingRelation`, avec un `rule` distinct (`RULE_AI_ACCEPTED`) toujours traçable comme d'origine IA — jamais automatique, jamais déclenché par la génération elle-même. |
| Double `FindingRelation` sur double acceptation | **PASS** | `accept_correlation_suggestion` revérifie l'état de la suggestion (`ACCEPTED` → no-op immédiat) et l'existence d'une relation déjà présente pour la même paire+règle avant d'insérer — vérifié explicitement (`test_accept_suggestion_creates_finding_relation_once`, deux appels consécutifs). |
| Rapport généré directement par le Report Agent, hors validation humaine | **PASS** | `ReportAgent` (lecture seule, même garde statique que le Correlation Agent) ne connaît ni `build_report_data()`, ni `render()`, ni le modèle `Report` — vérifié statiquement (imports absents de `app/api/routes/ai.py` pour la route `/reports/propose`, `test_propose_report_never_creates_a_report`) et en conditions réelles (rapport uniquement créé après clic humain sur "Generate", via le `POST /api/reports` existant et inchangé). |
| Identifiants (finding_id/job_id) inventés par le modèle | **PASS** | `CorrelationAgent.suggest()` et `ReportAgent.propose()` revalident chaque id retourné contre l'ensemble réellement transmis en argument — un id halluciné est silencieusement filtré, jamais transmis à la route appelante. Vérifié (`test_suggest_strips_hallucinated_finding_ids`, `test_propose_strips_hallucinated_job_ids`). |
| Routes non authentifiées | **PASS** | Les 9 nouvelles routes `/api/ai/missions/...`, `/api/ai/correlation-suggestions/...`, `/api/ai/reports/propose` ajoutées à `test_every_api_route_is_guarded_when_auth_enabled`. |
| Accès direct `Target`/écriture `authorization_status` depuis un agent | **PASS — inchangé, étendu** | Les garde-fous statiques existants (`test_ai_module_has_no_write_access_to_target_model`, `test_ai_module_has_no_subprocess_or_docker_access`) ne parcouraient que `app/ai/*.py` — étendus à un `rglob` recursif pour couvrir `app/ai/orchestrator.py` et `app/ai/agents/*.py`, ajoutés par cette phase. |

**Bug réel trouvé et corrigé pendant la vérification** (pas une faille de sécurité, mais une violation de contrainte DB qui aurait empêché toute Mission de progresser au-delà d'un outil invalide) : le premier jet de `advance_mission()` marquait une étape sans outil valide (halluciné ou rejeté par `prepare_job()`) comme `FAILED` — violant `ck_mission_step_job_id_required_when_executed`, qui exige un `job_id` pour ce statut. Corrigé en utilisant `SKIPPED` (déjà la sémantique correcte : "cette étape n'a jamais eu de `Job`"), qui n'exige pas de `job_id`. Trouvé par `tests/ai/test_orchestrator.py::test_step_with_hallucinated_tool_is_skipped_without_crashing`, qui a échoué avec une vraie `IntegrityError` PostgreSQL au premier lancement.

## Phase 19 — Mémoire IA par Projet/Asset : audit de sécurité

Surface ajoutée : `app/ai/memory.py`, `app/ai/agents/summary.py`, table `project_ai_summaries`, routes `GET/POST /api/ai/projects/{id}/summary(/regenerate)`, `ChatRequest.project_id`. Voir [phase-19-ai-memory.md](phase-19-ai-memory.md) pour l'architecture complète.

| Catégorie | Statut | Justification |
|---|---|---|
| Écriture par le Summary Agent | **PASS** | `SummaryAgent` (`app/ai/agents/summary.py`) n'a aucun accès `Session` (même garde statique que Correlation/Report Agent, `test_correlation_and_report_agents_have_no_db_session_access` — le nom du test est historique mais son `glob("*.py")` couvre tout `app/ai/agents/`, y compris `summary.py`, sans modification nécessaire). Seul `app/ai/memory.py` persiste, jamais l'agent lui-même. |
| Fuite de données entre projets | **PASS** | Toutes les requêtes de collecte (`Asset`, `Finding` via jointure `Job`, `AssetChangeEvent` via jointure `Asset`) sont scopées par `project_id` explicite — vérifié qu'aucune requête ne peut mélanger les données de deux projets (`test_regenerate_severity_counts_reflect_real_findings` utilise un projet isolé par test). |
| Isolation Job/Mémoire — un bug de régénération corrompt le résultat d'un Job | **PASS** | Même patron d'isolation que l'avancement de Mission (Phase 18) : hook dans le `finally` le plus externe d'`execute_job()`, session entièrement séparée, `try`/`except` dédié qui journalise et avale toute erreur sans jamais pouvoir réécrire `job.status`. Vérifié en forçant `regenerate_project_summary_for_job` à lever une exception : le Job reste `SUCCESS` (`test_job_stays_success_even_if_summary_regeneration_fails`). Vérifié également que le hook Mission et le hook Mémoire sont indépendants — la panne de l'un ne supprime jamais l'autre (`test_mission_hook_and_memory_hook_are_independent`). |
| Doublon de résumé (deux régénérations concurrentes) | **PASS** | `UniqueConstraint(project_id)` sur `project_ai_summaries` — un upsert (jamais un second `INSERT`) est la seule écriture possible ; vérifié (`test_regenerate_upserts_not_duplicates`). |
| Contournement du cooldown pour épuiser Ollama (DoS local) | **PASS — atténué, pas un vecteur externe** | Le cooldown de 5 minutes protège le hook automatique (`execute_job()`) contre un enchaînement de jobs régénérant à chaque fois ; la route manuelle le contourne intentionnellement mais reste derrière l'authentification API existante (`AUTH_ENABLED`), aucune route non authentifiée ne peut la déclencher en boucle. |
| Identifiants (project_id/job_id) invalides ou d'un autre projet | **PASS** | `regenerate_project_summary()` renvoie `None` (pas d'exception, pas de `Job`/`Project` orphelin créé) pour un `project_id` inconnu ; les routes traduisent ça en `404`. `based_on_job_id` provient exclusivement du `Job` réellement chargé par le hook, jamais d'une entrée utilisateur. |
| Injection dans le system prompt via des données de scan hostiles (titre de Finding, valeur de changement) | **PASS — cohérent avec le modèle existant** | Les données injectées dans le prompt (noms d'assets, valeurs de changement, résumé stocké) proviennent de sorties d'outils déjà scannés (potentiellement hostiles si la cible est compromise, même hypothèse que Phase 14/16) — jamais utilisées dans une requête SQL, un chemin de fichier, ou une commande. Le pire cas est une tentative de prompt injection contre le modèle lui-même, qui ne peut de toute façon exécuter aucune action (le chat n'a accès à aucun outil ni à la base de données, `CHAT_SYSTEM` inchangé). |
| Routes non authentifiées | **PASS** | Les 2 nouvelles routes ajoutées à `test_every_api_route_is_guarded_when_auth_enabled`. |
| Accès direct `Target`/écriture `authorization_status` depuis `app/ai/memory.py` ou `summary.py` | **PASS — inchangé** | Les garde-fous statiques récursifs (`rglob`, étendus en Phase 18) couvrent automatiquement les deux nouveaux fichiers sans modification. |

**Bug réel trouvé et corrigé pendant la vérification** (pas une faille de sécurité, mais une fragilité qui aurait silencieusement empêché la régénération de fonctionner dans certains contextes) : `asyncio.run()` pour ponter l'appel IA async depuis le hook sync échoue si une boucle asyncio est déjà active dans le thread appelant — absorbé silencieusement par le `except Exception` d'`execute_job()`, sans effet visible autre qu'un `RuntimeWarning: coroutine ... was never awaited`. N'affecte jamais la production réelle (le worker RQ n'a pas de boucle active), mais affectait silencieusement plusieurs tests préexistants qui appellent `execute_job()` depuis une fonction de test `async def`. Corrigé par `_run_coro()` (bascule vers un thread avec sa propre boucle si nécessaire) plutôt que de contourner le problème en changeant ces tests préexistants. Trouvé par l'apparition du `RuntimeWarning` sur la suite complète après l'ajout du hook, confirmé absent après correction.

**Aucune vulnérabilité nouvelle trouvée nécessitant un cycle REPRODUCE → DOCUMENT → FIX → TEST → RETEST pour cette phase** — la surface ajoutée (une table, des lectures récursives paramétrées, un déclenchement de rebuild via la queue existante) suit strictement les patterns déjà validés en Phases 13–16.

## Phase 20 — Evidence & Chain of Custody : audit de sécurité

Surface ajoutée : colonne `Job.evidence_sha256`, calcul inline dans `app/jobs/tasks.py::_execute_job`, `app/schemas/job.py::JobResponse.evidence_sha256`, composant frontend `EvidenceTimeline.vue`. Aucune nouvelle route, aucune nouvelle table.

| Catégorie | Statut | Justification |
|---|---|---|
| Falsification du hash par un client | **PASS** | `evidence_sha256` n'est jamais accepté en entrée d'aucune route — ni `JobCreateRequest`, ni aucun schéma d'écriture ne l'expose. Calculé exclusivement côté serveur, dans le worker RQ, à partir du `stdout` réellement reçu de l'agent Kali. |
| Hash calculé sur des données modifiées après coup | **PASS** | Calculé dans la même transaction, immédiatement après que `job.stdout` soit assigné depuis `raw.get("stdout")` — aucune fenêtre où `stdout` pourrait être modifié entre la réception et le hash. Vérifié en conditions réelles : hash recalculé indépendamment côté PostgreSQL (`encode(sha256(stdout::bytea), 'hex')`) correspond exactement à `evidence_sha256`. |
| `NULL` confondu avec un hash valide (ex. hash de chaîne vide) | **PASS** | Un `stdout` vide (`""`) obtient un hash bien défini (celui de la chaîne vide), distinct de `NULL` (aucune sortie n'a jamais existé, l'outil n'a jamais tourné) — vérifié explicitement (`test_execute_job_hash_of_empty_stdout_is_well_defined_not_none`). |
| Fuite de données entre Assets/Projects dans la timeline | **PASS — cohérent avec le modèle existant** | `EvidenceTimeline.vue` ne fait aucun appel réseau lui-même — elle affiche exactement les `jobs`/`findings` que la page parente a déjà chargés via les routes existantes (`GET /api/jobs?target_id=`/`?project_id=`, `GET /api/findings?target_id=`/`?project_id=`), déjà scopées et déjà auditées dans les phases précédentes. Aucun nouveau chemin de requête introduit. |
| XSS via stdout/titre de finding affiché dans la timeline | **PASS — inchangé** | Rendu via interpolation Vue `{{ }}` échappée par défaut, comme le reste de l'application — aucun `v-html` introduit par cette phase. |
| Exposition du hash complet à un utilisateur non authentifié | **PASS — cohérent avec le modèle existant** | `evidence_sha256` suit exactement le même contrôle d'accès que `stdout`/`stderr`/`result` déjà exposés par `GET /api/jobs/{id}` — aucune route n'a de contrôle d'accès distinct par champ (application mono-utilisateur sans RBAC, Phase 11), donc pas de nouvelle surface de fuite spécifique à ce champ. |
| Utilisation du hash comme preuve légale forte | **PASS — attente correctement calibrée** | Documenté explicitement (design + roadmap) comme "preuve d'intégrité minimale", pas une chaîne de custody cryptographiquement scellée (pas de signature, pas d'ancrage externe, pas de vault) — la base de données elle-même reste le seul système de confiance, cohérent avec le modèle de menace mono-utilisateur/local déjà établi (Phase 10/11). |

**Aucune vulnérabilité nouvelle trouvée nécessitant un cycle REPRODUCE → DOCUMENT → FIX → TEST → RETEST pour cette phase** — la surface ajoutée (une colonne, un calcul local synchrone, un composant de présentation pur sans appel réseau propre) est la plus petite de toutes les phases depuis la Phase 13, cohérente avec la complexité "S" annoncée par la roadmap.
